const user = args[0];
const target = args[1];

if (user.toLowerCase() === target.toLowerCase()) {
    `$(user) se abrazó a sí mismo... qué solito está 🥲`
} else {
    `Awww, ${user} se abrazó con ${touser} 💕`
}
